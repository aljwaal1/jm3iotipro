name: jamiyati
description: Arabic offline monthly association manager.
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter
  shared_preferences: ^2.3.3
  url_launcher: ^6.3.1

flutter:
  uses-material-design: true
